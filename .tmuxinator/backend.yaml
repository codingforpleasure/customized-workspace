project_name: Tmuxinator
project_root: ~/repos/a.blazemeter.com
socket_name: foo # Not needed. Remove to use default socket rvm: 1.9.2@rails_project
tabs:
- editor:
layout: main-vertical panes:
        - ls
        - #empty, will just run plain bash
        - top
- shell: junk
- logs:  echo "logs welcome"
